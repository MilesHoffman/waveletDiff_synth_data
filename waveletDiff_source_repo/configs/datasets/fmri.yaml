# fMRI dataset specific configuration
dataset:
  name: "fmri"
  seq_len: 24

# Dataset-specific training adjustments
training:
  batch_size: 512
  epochs: 5000

# Wavelet settings optimized for fMRI
wavelet:
  type: "db2"
  levels: "auto"



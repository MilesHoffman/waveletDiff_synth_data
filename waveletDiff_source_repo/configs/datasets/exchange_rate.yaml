# Exchange Rate dataset specific configuration
dataset:
  name: "exchange_rate"
  seq_len: 24

# Dataset-specific training adjustments
training:
  batch_size: 512
  epochs: 5000

# Wavelet settings optimized for exchange rate
wavelet:
  type: "db2"
  levels: "auto"


